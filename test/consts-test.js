describe('Constants', function() {
  it('Should have the correct value for ENTER_KEY', function() {
    expect(ENTER_KEY).toEqual(13);
  });

  it('Should have the correct value for MONTH_MAY', function() {
    expect(MONTH_MAY).toEqual(5);
  });
});